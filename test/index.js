'use strict';

// Load modules

const Lab = require('@hapi/lab');


// Declare internals

const internals = {};


// Test shortcuts

const lab = exports.lab = Lab.script();
const { describe, it, expect } = lab;


describe('objection-joi', () => {

    it('should', async () => {

        expect(1).to.equal(1);
    });
});
